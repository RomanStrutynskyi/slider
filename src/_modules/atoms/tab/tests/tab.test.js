'use strict';

import Tab from '../tab';

describe('Tab View', function() {

  beforeEach(() => {
    this.tab = new Tab();
  });

  it('Should run a few assertions', () => {
    expect(this.tab);
  });

});
