'use strict';

import SliderItem from '../slider-item';

describe('SliderItem View', function() {

  beforeEach(() => {
    this.sliderItem = new SliderItem();
  });

  it('Should run a few assertions', () => {
    expect(this.sliderItem);
  });

});
