'use strict';

import SliderImg from '../slider-img';

describe('SliderImg View', function() {

  beforeEach(() => {
    this.sliderImg = new SliderImg();
  });

  it('Should run a few assertions', () => {
    expect(this.sliderImg);
  });

});
